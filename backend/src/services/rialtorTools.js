// Archivo simplificado: herramientas eliminadas para resetear el chat a foja cero
// Mantener solo si se necesitan en el futuro, pero por ahora vacío

module.exports = {
  // Funciones eliminadas: kb_lookup, tasador_express, calc_gastos_escritura, calc_honorarios
};
