const express = require('express');
const router = express.Router();

// Placeholder para rutas de categorías
router.get('/', (req, res) => {
  res.json({ message: 'Categories routes - To be implemented' });
});

module.exports = router;
