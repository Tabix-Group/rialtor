const express = require('express');
const router = express.Router();

// Placeholder para rutas de calculadora
router.get('/', (req, res) => {
  res.json({ message: 'Calculator routes - To be implemented' });
});

module.exports = router;
