const express = require('express');
const router = express.Router();

// Placeholder para rutas de documentos
router.get('/', (req, res) => {
  res.json({ message: 'Documents routes - To be implemented' });
});

module.exports = router;
